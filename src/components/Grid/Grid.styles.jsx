import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    flexGrow: 1,
    marginTop: '20px'
  },
  paper: {
    height: 140,
    width: 100
  },
  wrapper: {
    marginBottom: '20px'
  }
}));

export default useStyles;

